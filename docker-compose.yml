version: "3.9"

services:
  web:
    build: .
    container_name: mdu-global-fair-web
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
    restart: unless-stopped
